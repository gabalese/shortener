web:
    build: app
    environment:
        DJANGO_SETTINGS_MODULE: shortner.settings.docker
    working_dir: /code/shortner
    command: sh /code/docker/django-init.sh
    volumes:
        - .:/code
    ports:
        - "8000:8000"
